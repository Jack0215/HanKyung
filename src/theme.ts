// Mirrors --navy-900 in src/index.scss. Kept in sync manually: antd's
// ConfigProvider theme tokens are plain JS values and can't read CSS
// custom properties, so this is the single JS-side source of truth for
// the brand navy used in antd component theming.
export const BRAND_NAVY = '#0f2745'
